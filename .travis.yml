language: python
python:
  - "3.9"
before_install:
  - python --version
  - pip install -U pip
# command to install dependencies
install:
  - pip install -r requirements.txt
# command to run tests
script:
  - python unit_tests/test_kebele.py
  
